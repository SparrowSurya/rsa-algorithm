# RSA Algorithm Demonstration

This describes the working of the rsa algorithm to generate `public key` and `private key`.

**NOTE**: keep value `PRIME_LENGTH` low, as it becomes computationally expensive for encrypt & decrypt.
